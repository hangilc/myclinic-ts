export enum HorizDirection { Left, Center, Right }
export enum HorizAlign { Left, Center, Right, Justify }
export enum VertDirection { Top, Center, Bottom }
export enum VertAlign { Top, Center, Bottom }
